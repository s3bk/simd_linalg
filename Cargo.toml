[package]
name = "simd_linalg"
version = "0.1.0"
authors = ["Sebastian K <s3bk@protonmail.com>"]
edition = "2018"

[features]
alloc = []
std = ["alloc"]
default = ["std"]

[dependencies]
packed_simd = { version = "*", package = "packed_simd_2" }

[dev-dependencies]
rand = "*"
